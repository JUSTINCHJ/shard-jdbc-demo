
insert into t_address
(
        id,
        code,
        name,
        pid,
        type,
        lit
)
values
(
        :address.id,
        :address.code,
        :address.name,
        :address.pid,
        :address.type,
        :address.lit
)
